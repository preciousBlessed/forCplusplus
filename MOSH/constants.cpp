#include <iostream>

int main()
{
    // To make a variable a constant, use the const keyword before declaring the variable
    const double pi = 3.14;
    // pi =0; wont won't because, it's a constant
    return 0;
}